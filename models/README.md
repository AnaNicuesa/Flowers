# Models

Trained model checkpoints are saved to `checkpoints/` and are not committed to version
control (large binary files — see `.gitignore`). Training scripts/notebooks will document
how to regenerate them from scratch.
